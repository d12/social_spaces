
<%= react_component("Clicker", { groupId: @group.key, instanceId: @instance.id, userId: @user.id, bootstrapData: @bootstrap_data, users: @group.users, meetUrl: @group.meet_url, jitsiJwt: @user.jitsi_jwt }) %>

<br/><br/>

<% # TODO: This should be present but disabled if you're not the host %>
<% # TODO: This should be react %>
<% # TODO: This should be a button %>
<% if @user == @group.host %>
  <%= link_to "End activity", leave_activity_path, method: :delete %>
<% end %>
