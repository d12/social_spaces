<%= react_component("ActivityIndex", {
      userName: @user.name,
      groupId: @group.key,
      meetUrl: @group.meet_url,
      activities: @activities,
      users: @group.users,
      jitsiJwt: @user.jitsi_jwt,
      alertToast: alert,
      noticeToast: notice }) %>
