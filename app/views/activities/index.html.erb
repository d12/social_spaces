<%= react_component("Greeting", { userName: @user.name }) %>

You're in group: <%= @group.key %>
<br>
<%= link_to "Google Meet video call", @group.meet_url %>

<br><br>

<% @activities.each do |activity| %>
    - Join activity: <%= link_to activity.display_name, join_activity_path(activity: activity.slug), method: :post %>
    </br>
<% end %>

- <%= link_to "Leave group", leave_group_path, method: :delete %>
<br>
- <%= link_to "Log out", logout_path, method: :delete %>

<%= react_component("GroupMembers", { groupId: @group.key, initialUsers: @group.users }) %>
