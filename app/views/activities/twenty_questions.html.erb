<% # TODO: This user_id should be encrypted or something, it's too easy to spoof someone else %>
<%= react_component("TwentyQuestions", { groupId: @group.key, instanceId: @instance.id, userId: @user.id, bootstrapData: @bootstrap_data, users: @group.users, meetUrl: @group.meet_url }) %>
<br><br>
<% if @user == @group.host %>
  <%= link_to "End activity", leave_activity_path, method: :delete %>
<% end %>
