<%= react_component("TwoTruthsOneLie", { groupId: @group.key, instanceId: @instance.id, userId: @user.id, bootstrapData: @bootstrap_data, meetUrl: @group.meet_url, users: @group.users, jitsiJwt: @user.jitsi_jwt }) %>
<br><br>
<% if @user == @group.host %>
  <%= link_to "End activity", leave_activity_path, method: :delete %>
<% end %>
